{
  "name": "proxy-estado-scraper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "install:playwright": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "playwright": "^1.48.2"
  }
}
